- provide :page_title, model.title

%p
  %b= _('Event|Start date')
  = model.next_date.start_date || '-'
%p
  %b= _('Event|End date')
  = model.next_date.end_date || '-'

%hr
= markdown(model.description)