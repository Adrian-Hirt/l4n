.card
  = simple_form_for [:admin, model] do |f|
    .card-body
      .row
        .col-md-6
          = f.input :name
        .col-md-6
          = f.input :api_key, disabled: true, placeholder: _('Admin|API Application|Key will be generated when saved')
    .card-footer
      = f.save
