.row
  - User::PERMISSION_FIELDS.each do |permission_field|
    - users_with_permissions = User.where(permission_field => true)
    .col-md-6.mb-3
      .card
        .card-header.d-flex.justify-content-between.align-items-center
          = permission_field.to_s.humanize
          .badge.bg-primary= users_with_permissions.count
        .card-body
          - users_with_permissions.each do |user|
            = link_to user.username, permissions_admin_user_path(user)
            %br
